apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: vmall
  name: nginx-app-pvc
spec:
  storageClassName: standard-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi